const adultsSelector = (state) => state.filter.adultsOptionsSelect;
const departureSelector = (state) => state.filter.departureCityInitial;
const arrivalSelector = (state) => state.filter.arrivalCityInitial;
export { adultsSelector, departureSelector, arrivalSelector };
